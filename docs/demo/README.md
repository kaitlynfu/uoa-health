# Programme Explorer Demo

Open `programme-explorer-preview.html` in a web browser to try the standalone
Programme and Career Explorer concept.

The demo uses an embedded snapshot of the backend data, so it does not require
FastAPI or Expo to be running. It is intentionally separate from the production
React Native frontend and will not update when the database changes.
